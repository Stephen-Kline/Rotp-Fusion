# Domain Docs

Layout: single-context

- Domain language: `CONTEXT.md` at the repo root
- Architectural decisions: `docs/adr/` at the repo root

When reading context, load `CONTEXT.md` first, then any relevant ADRs from `docs/adr/`.
